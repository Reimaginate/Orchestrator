# Reimaginate.Orchestrator.DSL

YAML DSL parsing and validation support for Orchestrator workflow definitions.

This package contains the models and parsing infrastructure used to turn `.workflow.yaml` files into Orchestrator workflow definitions.
